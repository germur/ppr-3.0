# ============================================================
# _redirects — Paramount Property Restoration
# ============================================================
# VACIADO en el replanteamiento SEO v2 (2026-06).
# Toda la lógica de redirects (poda 410, consolidaciones 301,
# ciudades conservadas, trailing slash, fallback 404) vive ahora
# en netlify.toml — fuente única de verdad.
#
# Netlify procesa _redirects ANTES que netlify.toml, por eso este
# archivo DEBE quedar vacío: si revive el sistema viejo (11k reglas),
# sus reglas 200 pisan los 410 de la poda y la matriz programática
# vuelve a estar viva. NO regenerar con scripts/generate-redirects.mjs.
# ============================================================
